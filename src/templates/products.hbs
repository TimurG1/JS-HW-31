<h2>Products</h2>
<ul>
  {{#each products}}
    <li>
      <h2 class="name">{{name}}</h2>
      <p class="price">Price:{{price}}</p>
      <p class="description">{{description}}</p>
    </li>
  {{/each}}
</ul>
